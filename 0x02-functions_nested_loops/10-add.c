#include <stdio.h>
#include "main.h"
/**
* add - add two numbers
* @a: add int to int b
* @b: add int to int a
* Return: the sum of the numbers
*/
int add(int a, int b)
{
int k;
k = a + b;
return (k);
}
