#include <stdio.h>
#include "main.h"
/**
* _isalpha - check if an alphabet is in uppercase
* @c: check character c
* Return: 1 if uppercase and 0 if not
*/
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
