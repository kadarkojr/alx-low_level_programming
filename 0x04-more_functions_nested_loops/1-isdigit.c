#include <stdio.h>
#include "main.h"
/**
* _isdigit - check for a digit (0 through 9)
* @c: check int c
* Return: 1 if c is a digit and 0 if not
*/
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
