#include <stdio.h>
#include "main.h"
/**
* _isdigit - check if an alphabet is in uppercase
* @c: check character c
* Return: 1 if uppercase and 0 if not
*/
int _isdigit(int c)
{
return (c >= 0 && c <= 9);
}
