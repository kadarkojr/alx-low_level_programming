#include <stdio.h>
#include <string.h>
#include "main.h"

/**
* _strlen - print the length of a string
* @s: char whose length is being checked
* Return: Return length of s
*/

int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);
}
